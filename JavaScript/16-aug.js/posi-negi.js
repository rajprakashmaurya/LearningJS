// let arr = [1, 3, 5, -6, 54, -3, 0, 23, -34, 4];
// function findnegativeandpositive(arr) {
//   let negative = [];
//   let positive = [];
//   for (let i = 0; i < arr.length; i++) {
//     if (arr[i] > 0) {
//       positive.push(arr[i]);
//     } else if (arr[i] < 0) {
//       negative.push(arr[i]);
//     } else {
//       positive.push(arr[i]);
//     }
//   }
//   return `positive arr is ${positive} and negative arr is ${negative}`;
// }

// console.log(findnegativeandpositive(arr));

let nums = [2, 345, 54, , 456, -34534, 4];
let neg = [];
let posi = [];
for (let nums of num) {
  if (nums > 0) posi.push(nums);
  else neg.push(nums);
}
//return neg(), posi();
console.log(nums);
